# Edward 项目规则

## 产品名称

- 软件英文名称：Edward。
- 软件中文名称：剪刀手。

## 窗口标题栏

- macOS 保留系统原生红、黄、绿窗口控制；Windows 保留系统原生最小化、还原/最大化与关闭按钮。
- 所有界面的系统原生标题栏固定深色模式；不得在应用内页面重复设置或模拟一条标题栏。
- 必须保留系统原生标题栏双击触发的窗口缩放动作。

## 第一性设计原则

所有架构、引擎、界面和实现决策，必须优先满足以下目标：稳定性、低资源占用、兼容性、跨平台能力、画质和模块化。出现取舍时，不得为了短期开发速度牺牲这些原则；应明确说明取舍并选择更可靠的方案。

- 不得因依赖、模型或工具尚未下载完成，或下载速度慢，而降低性能或质量标准，或采用劣化方案绕过问题；必须等待下载完成或解决下载与配置问题，并严格坚持最佳性能和质量目标。

## 统一画布坐标规范（强制）

所有设计、数据合同、属性检查器、时间线插入、预览、拖拽命中、组件运行时、渲染和导出必须使用同一套画布坐标：画布中心是 `(0, 0)`；水平向右为正、向左为负；垂直向上为正、向下为负。任何屏幕坐标、DOM 坐标、Canvas 像素坐标或导出分辨率坐标都必须在边界处显式转换，禁止在模块内部重新定义原点或 Y 轴方向。

- `x/y` 属性、关键帧、偏好、时间线插入初始位置和导出相对坐标均表示上述中心坐标。
- 标尺、属性检查器、素材拖入、直接操作、预览、React/SVG/HTML 组件和离屏导出必须共享同一换算函数或等价的可证明公式。
- 导出分辨率变化只能改变比例，不能改变中心原点或坐标正负语义。
- 新增任何位置相关字段前，必须补充至少一个正负方向、中心点和预览/导出一致性的回归测试；发现不一致必须先统一坐标合同，再继续功能开发。

## 绝对禁止的技术路线

- 严禁使用 Component IR、Component IR 转换层、理解层或任何等价的中间转换层来实现组件预览、参数编辑、渲染或导出。组件必须由其原生运行时直接负责预览与输出，并使用同一份原生参数数据。
- 严禁使用 OpenShot、DaVinci Resolve Studio、Premiere 及其 API、插件、脚本或通信方式作为 Edward 的预览、编辑、渲染、导出、资源入库或组件运行依赖。

## 问题闭环执行

- 发现问题后必须继续定位根因并设法解决，不得停顿在中间结果。
- 即使发现技术阻塞，也必须继续分析、试验和验证可行解决路径，不得以阻塞为理由停顿或结束任务。
- 如果实机验证仍然失败，必须继续修改、重跑验证并追踪到根因解决；“仍未完成”只能作为内部状态，不能成为停止推进的结论。
- 在未达到验收标准时继续静默推进，不向用户报告“仍未达到”等过程状态；只有最终达标后才汇报结果。
- 不需要向用户报告过程节点；默认跳过中间进度输出，持续推进并解决任务，用户只关心最终实现和验证结果。
- 不得将用户明确要求拆成打折、近似或未验证的交付；必须严格按照命令执行到位。
- 只有实现、验证和用户可见结果均达到要求后，才可以停止并报告完成；遇到技术阻塞时必须继续排查可行路径，并明确记录阻塞证据，不能把阻塞当作完成。
- 对有明确验收节点的任务，开始工作时必须创建 `.edward/acceptance/current.json` 并列出可执行检查；未将其标记为通过前不得结束任务。停止钩子以该文件为硬性完成门。

## 操作记录

- 每类操作必须在项目目录内维护一份 Markdown 记录；后续同类操作只追加本次结果，不为每次操作新建文件。记录应说明操作时间、目的、涉及文件或外部状态、结果与验证情况。

## 开源与商业边界

- 程序代码托管在 GitHub 并开源。
- 资源库独立托管在其他服务，不随程序代码仓库分发。
- 成品软件与资源库采用订阅制收费。涉及 GPL 代码时，订阅设计不得额外限制用户对 GPL 代码享有的运行、修改和再分发权利；订阅应对应资源库、云端服务、官方发布与支持等独立服务价值。
