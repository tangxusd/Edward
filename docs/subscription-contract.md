# 订阅授权客户端契约

## 已确认规则

- 新账号可免费使用 7 天；后续按月、季或年订阅。
- 每个账号最多绑定 3 台设备，同一时刻只允许一个活动会话。
- 最近一次有效校验后的离线编辑窗口为 2 小时。
- 触发 AI 分析前必须在线校验订阅；有效窗口内不重复校验。

## 客户端所需响应

授权服务需返回账号 ID、不可逆设备哈希、会话 ID、状态、校验时间和有效截止时间。客户端必须拒绝账号或设备哈希与本地缓存不一致的响应。

## 服务端职责

服务端负责试用资格、订阅状态、三设备上限和活动会话替换。客户端不得自行判断付费资格或制造有效授权缓存。
