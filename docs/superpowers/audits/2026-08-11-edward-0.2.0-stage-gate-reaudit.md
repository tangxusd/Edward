# Edward 0.2.0 阶段门修复后复审

**日期：** 2026-08-11（Asia/Shanghai）
**对象：** `2026-08-11-edward-0.2.0-stage-gate-audit.md` 的 P0/P1/P2 结论。
**结论：** **P0 为 0，可以开始 00“规格与核心合同”实施。** 仍有 4 个明确的阶段前置门，但它们不阻塞 00–04；不得越过其对应阶段。

## 原 P0 修复核验

| 原编号 | 修复 | 复审证据 | 状态 |
| --- | --- | --- | --- |
| P0-01 | 时间线唯一视觉基准归并为存在的工作台整合稿；缺失过程稿明确禁止作为输入。 | 最终表 HTML 44 个、内容目录 44 个、Route 基线 44 条，双向差集均为空。 | 通过 |
| P0-02 | 早期 core-engine 计划改为“历史，禁止执行”；00–08 为唯一执行入口。 | 旧文件首段禁止派生任务；活动计划/规格/合同不再引用它。 | 通过 |
| P0-03 | `uiState` 拆为 `guides`、`markers`、`aiSessions`、`resourceLocks`、`commandHistory`；会话视图状态显式禁止落盘。 | 权威 PRJ-04、00 `ProjectDocument` 合同和台账已一致。 | 通过 |
| P0-04 | 明确原始字节只存 `media/`，色彩转换为 RenderGraph/派生代理的非破坏性操作。 | ARC-03、00 全局约束和 `render-graph.md` 一致。 | 通过 |
| P0-05 | 冻结唯一 RenderGraph、不可变 `RenderScene`、QML 交互叠层与 MLT/FFmpeg 合成边界。 | `docs/contracts/render-graph.md`。 | 通过 |
| P0-06 | 锁定 whisper.cpp、本地中英文转写、时间戳、模型下载失败语义、性能夹具和禁止降级规则。 | `dependency-lock.md`、`local-transcription-and-document-import.md`、06 计划。 | 通过 |
| P0-07 | 锁定 DOCX 的 libzip/pugixml 与文本 PDF 的 Poppler 路径、输入安全边界和失败语义。 | 同上文稿合同与依赖锁定。 | 通过 |
| P0-08 | 加入 Qt Network、libsodium、受限宿主 RPC、域名白名单、凭证隔离与临时目录边界。 | `dependency-lock.md`、`plugin-host-rpc.md`、06 计划。 | 通过 |

## 本次可执行静态验证

```text
最终视觉表 HTML：44
最终视觉表缺失文件：0
Route 基线：44
最终表缺少 Route：0
Route 多余文件：0
活动计划引用旧 core-engine：0
活动合同中的泛化 uiState 持久化声明：0
新增合同文件：4/4 存在且非空
git diff --check -- docs：通过
```

## macOS 本机运行时依赖复核（2026-08-11 修复后）

本次只关闭开发机上文稿解析、签名验证和本地转写运行时依赖的缺失；不把“已安装库”误写成模型、字体、硬件质量或 Windows 发布门已经通过。

| 依赖/能力 | 实测命令 | 实测结果 | 状态 |
| --- | --- | --- | --- |
| `whisper-cpp` | `brew list --versions whisper-cpp` | `1.9.2` | 通过 |
| DOCX 归档读取 | `pkg-config --modversion libzip` | `1.11.4` | 通过 |
| DOCX XML 读取 | `pkg-config --modversion pugixml` | `1.16` | 通过 |
| 可复制文本 PDF | `pdftotext -v` | `26.08.0` | 通过 |
| 插件签名验证 | `pkg-config --modversion libsodium` | `1.0.22` | 通过 |

安装使用中科大 Homebrew bottles 镜像；阿里云镜像对 Poppler 26.08.0 缺少瓶包，未因此删除 Poppler 或换用功能较弱的 PDF 路径。

## 复验：视觉清单与 Route 基线的唯一输入范围

视觉确认清单同时记录最终表格与“覆盖与禁止事项”中的历史文件名。复验只能抽取 `## 页面与最终视觉稿` 表格至下一节标题之间的 `.html` 路径；不得对整个 Markdown 做全局文件名扫描。

| 检查 | 结果 |
| --- | --- |
| 最终表格唯一 HTML 文件 | 44 |
| `visual-route-baseline.md` Route 行 | 44 |
| 仅在最终表格中 | 0 |
| 仅在 Route 基线中 | 0 |
| 44 个最终文件在 `.superpowers/brainstorm/19764-1786246498/content/` 中存在 | 通过 |

该复验修正了全局扫描会错误计入历史覆盖稿的统计方法；结论未改变：最终视觉输入与 Route 基线是一一对应关系。

## 剩余阶段前置门（非 P0）

| 门 | 必须在何时关闭 | 当前确定规则 |
| --- | --- | --- |
| G-01 字体包 | 05 工作台视觉实现前 | 选择并记录可再分发的中英文字体、字重、SHA-256 和跨平台回退链；不得使用仅 macOS 可分发的 SF 字体作为 Windows 正文回退。 |
| G-02 转写模型工件 | 06 AI/创建实现前 | 下载锁定的 `large-v3-turbo` GGML 工件，记录实际文件名、许可证和 SHA-256 到本地依赖 manifest；未完成下载不得切换小模型。 |
| G-03 硬件编码质量门 | 07 导出实现前 | 对 VideoToolbox/NVENC/QSV/AMF/Media Foundation 按高/中/低档建立实测输出质量、码率与软件回退阈值；未达成同质量语义的后端不得启用。 |
| G-04 Windows 可复现媒体前缀 | 08 双平台发布验证前 | 用 MSVC x64 构建锁定 FFmpeg/MLT/whisper.cpp/libzip/pugixml/Poppler/libsodium，生成依赖 manifest 与部署 smoke；不混用 MinGW/DLL。 |

## 未改变的边界

- 不创建或恢复旧 Electron/TypeScript/Python 代码；本轮仅修订设计、合同、计划和安装指南。
- 后端服务仍未实现。账户、订阅、资源审核、支付和设备管理的客户端只能在真实服务/本地签名夹具存在时报告成功。
- 当前仓库尚无 CMake 工程与源文件，因此本轮没有可运行的 CTest；静态合同验证不等价于应用构建验证。
