# Edward 0.2.0 工作台与资源库实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` task-by-task.

**Goal:** 将已确认工作台、资源 Tab、资源升级和设置/导出入口实现为 Qt/QML 界面。

### Task 1: 原生壳与四栏布局

**Files:** Create `src/desktop/main.cpp`, `src/desktop/qml/Main.qml`, `src/desktop/qml/Workbench.qml`, `src/desktop/qml/NativeTitlebar.qml`, `src/desktop/qml/DesignTokens.qml`, `src/desktop/include/edward/desktop/workbench_controller.hpp`, `src/desktop/src/workbench_controller.cpp`, `tests/desktop/test_workbench_layout.cpp`, `tests/desktop/test_native_titlebar.cpp`.

**Consumes:** 权威规格 UI-01 至 UI-04、`ProjectDocument` 只读快照。

**Produces:** `WorkbenchController::layout()`、`Workbench.qml` 的 `workbenchRoot`、`timelinePane`、`rightPane` objectName。

- [ ] **Step 1: 写失败测试**:

```cpp
void test_RightPanelAndTimelineAttachToBottom() { QCOMPARE(layout.rightBottom(), layout.windowBottom()); }
```

- [ ] **Step 2: 验证失败**：运行 `ctest --preset macos-debug -R desktop.workbench_layout --output-on-failure`，预期失败。
- [ ] **Step 3: 最小实现**：实现原生深色标题栏安全区、左 15%/中间/右 15%/底部 20%、三条可拖拽分隔线；标题栏仅保存、项目名、模型、设置、导出。macOS/Windows 的原生控制、双击缩放和安全区必须遵从 `native-titlebar-bridge.md`；页面不得自绘第二标题栏。
- [ ] **Step 4: 验证通过**：重跑测试并做 QML 截图回归，预期无内层标题栏、右栏触底、时间线触底、左中分隔线不贯穿底部时间线。
- [ ] **Step 5: 提交**：`git add src/desktop tests/desktop && git commit -m "feat: add native workbench shell"`。

### Task 2: 八个资源 Tab 与本地资源索引

**Files:** Create `src/resources/include/edward/resources/resource_index.hpp`, `src/resources/src/resource_index.cpp`, `src/resources/include/edward/resources/resource_catalog.hpp`, `src/resources/src/resource_catalog.cpp`, `src/desktop/qml/ResourcePanel.qml`, `tests/resources/test_resource_index.cpp`, `tests/resources/test_resource_catalog_view.cpp`.

**Consumes:** 权威规格 RES-01/RES-02、媒体导入结果和由 00 合同定义的本地资源索引格式。06-4 以后再将同一 `ResourceCatalog` 接到验签远端目录；05 不假定该网络实现已存在。

**Produces:** `ResourceIndex::category(ResourceId)`、`ResourceIndex::isInstalled(ResourceId)`、`ResourcePanel.qml`。

- [ ] **Step 1: 写失败测试**:

```cpp
void test_ImportedMediaGoesToImportedNotMine() { QCOMPARE(index.category(id), Category::Imported); }
void test_ExistingResourceDisablesDownload() { QVERIFY(index.isInstalled(resource)); }
```

- [ ] **Step 2: 验证失败**：运行 `ctest --preset macos-debug -R resources.index --output-on-failure`，预期失败。
- [ ] **Step 3: 最小实现**：实现媒体“导入/我的/AI 生成”手风琴和其他 Tab“我的/默认”；为随安装包的本地默认资源实现一级/二级分类、静态预览和本地已装灰态。把单资源下载、分类下载定义为 `ResourceCatalog` 的不可用真实失败态，不伪造联网成功；06-4 完成后只修改该接口的目录提供者以接入验签目录。媒体 Tab 不读取云素材；普通资源不能从当前页面创建；资源添加到时间线后才生成可编辑实例。
- [ ] **Step 4: 验证通过**：重跑测试与资源 Tab 截图，预期图标、分类顺序、导入素材归类、下载状态和 18×18 图标盒吻合确认稿。
- [ ] **Step 5: 提交**：`git add src/resources src/desktop tests && git commit -m "feat: add local resource library"`。

### Task 3: 设置、资源升级与视觉回归

**Files:** Create `src/desktop/qml/SettingsDialog.qml`, `src/desktop/qml/ModelEditor.qml`, `src/desktop/qml/ResourceUpgrade.qml`, `src/desktop/qml/ResourcePreview.qml`, `src/desktop/qml/ExportDialog.qml`, `src/desktop/qml/AccountDialog.qml`, `src/desktop/qml/SubscriptionDialog.qml`, `src/desktop/qml/DeviceManagement.qml`, `src/desktop/qml/PaymentConfirm.qml`, `docs/contracts/visual-routes.json`, `tests/desktop/test_visual_manifest.cpp`.

**Consumes:** 权威规格第 8 节、资源/模型/账户客户端状态、`DesignTokens.qml`。

**Produces:** 每项最终视觉稿的固定 QML route、objectName、截图基线和失败态。

- [ ] **Step 1: 写失败测试**:

```cpp
void test_EveryConfirmedPageHasARegisteredQmlRoute() { QVERIFY(routes.coverManifest()); }
```

- [ ] **Step 2: 验证失败**：运行 `ctest --preset macos-debug -R desktop.visual_manifest --output-on-failure`，预期失败。
- [ ] **Step 3: 最小实现**：本任务截图前必须关闭 G-01：`assets/fonts/` 和 `docs/contracts/font-manifest.json` 已具备同一可再分发字体、许可证、版本与 SHA-256。实现项目/模型/插件设置 Tab、登录/注册/验证码、订阅/设备/支付的真实客户端失败入口、资源升级预览与关闭入口，导出入口只从标题栏打开；标题栏必须遵从 `native-titlebar-bridge.md`。加载每个已确认视觉状态的截图基线。`DesignTokens.qml` 固定深色颜色、18×18 图标可见盒、字体回退、青色选中态、分隔线和截图窗口尺寸，禁止各页面自行猜测样式。创建 `docs/contracts/visual-routes.json`，其内容必须逐项复制 `docs/contracts/visual-route-baseline.md` 的 HTML、route、QML objectName、截图 ID、固定窗口尺寸与允许像素差；`test_visual_manifest.cpp` 必须读取该文件而非只判断页面数量。
- [ ] **Step 4: 验证通过**：重跑测试，预期所有清单页面有路由、截图基线和可呈现的真实失败态。
- [ ] **Step 5: 提交**：`git add src/desktop tests docs/contracts && git commit -m "feat: implement confirmed workbench surfaces"`。

## 验收门

工作台不能切换项目；资源模板只能添加后编辑实例；界面不出现云素材或公开组件分类。
